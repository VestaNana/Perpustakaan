@model PerpustakaanViewModel.PendaftaranViewModel

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Pendaftaran</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.KodeKartu)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.KodeKartu)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.KodePetugas)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.KodePetugas)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.TglPembuatan)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TglPembuatan)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.TglExpired)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TglExpired)
        </dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="button" value="Delete" class="btn btn-danger btn-delete-conf" data-id="@Model.Id" />
            <input type="button" value="Cancel" class="btn btn-default" data-dismiss="modal" />
        </div>
    }
</div>

<script type="text/javascript">
    $(".btn-delete-conf").click(function () {
        //alert('Id-nya adalah :' + $(this).attr('data-id'));
        $.ajax({
            url: '@Url.Action("DeleteConfirm","Pendaftaran")',
            type: 'post',
            data: { id: $(this).attr('data-id') },
            success : function (result)
            {
                if (result.success) {
                    $("#modal-form").modal("hide");
                    ShowList();
                }
                else {
                    alert('Error :' + result.Message);
                }
            }
        })
    });

</script>